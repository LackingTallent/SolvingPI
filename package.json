{
  "name": "solving-pi-v9",
  "version": "0.1.0",
  "description": "Solving PI v9 \u2014 EVE Online Planetary Industry planner. Ground-up rebuild: verified game physics, one universal ledger, judge-validated plans.",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "tsx --test tests/*.test.ts",
    "gate2": "npm run gate1",
    "gen-sde": "node tools/gen-sde.mjs",
    "build": "node tools/build.mjs",
    "smoke": "node tools/smoke.mjs",
    "gate7": "npm run gate1 && npm run build && npm run smoke",
    "typecheck": "tsc --noEmit",
    "gate1": "npm run typecheck && npm run test",
    "matrix": "tsx tools/matrix.ts"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}